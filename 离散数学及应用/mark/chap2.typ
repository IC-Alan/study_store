= 函数
多对一或者一对一 B可能没占满
  ∅是空集
== 单射 one-to-one function = injection 只有一对一 且可能B没占满
== 满射（映上函数） onto function = surjection B都占满了 (可能多对一或一对一)
== 双射 one-to-one correspondence = bijection 一对一且占满
#image("image-15.png")
= The Size of a Set 记作$abs(S)$
= 幂集Power Sets
集合 S 的所有子集构成的集合。记作P(S) = ${A divides A subset.eq S}$ 包括空集

$P(A) in P(B) arrow A in B$

$P(A) subset.eq P(B) arrow.l.r A subset.eq B$

= ceiling function and floor function
#image("image-1.png")
#image("image.png")
= finite sets 有限集合
设 S 是一个集合。如果 S 中恰好有 n 个不同的元素，其中 n 是一个非负整数，那么我们说 S 是一个有限集（finite set），并且 n 是 S 的基数（cardinality）。
= 集合可数Countable Sets
如果一个集合：
- 是有限集
- 或者，与正整数集$Z^+$有相同的基数的无限集
则这个集合是可数的 (countable)，否则是不可数的 (uncountable)

*判断方法：若集合可数，则存在一个双射$g: N arrow A $( 即A的元素可以排成 $a_1,a_2,a_3……$) $a_n=f(n)$* 

if $P(A) in P(B)$, then $A in B$

PROOF：

由 $P(A) in P(B) arrow P(A) subset.eq B$

$A subset.eq A arrow A in P(A)$

$P(A) subset.eq B and A in P(A) arrow A in B$

= Set Operations

== 补集 (complement)
$overline(A)$

== 差集 (difference)

#image("image-175.png",height:4cm)
== 对称差集 (symmetric difference)
#image("image-176.png",height:4cm)

